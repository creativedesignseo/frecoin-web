# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Netlify folder
.netlify

# Client communications — internal, never publish to this public repo
correos/
.playwright-mcp/

# Backoffice CMS — SERVER-ONLY secrets (NEVER commit — this is a PUBLIC repo)
public/admin/api/config.php
**/admin/api/config.php
# User-uploaded media — lives on the server, not in git
public/assets/uploads/
# PHP per-dir overrides may carry env-specific limits
public/**/.user.ini
